# Base da API. Em dev deixe vazio: o proxy do Vite (/api -> backend) cuida disso,
# mantendo frontend e API "same-origin" para o cookie httpOnly do refresh funcionar.
VITE_API_BASE_URL=

# URL do backend para o proxy de dev (default http://localhost:8080).
VITE_BACKEND_URL=http://localhost:8080
